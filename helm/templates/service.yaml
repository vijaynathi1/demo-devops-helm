apiVersion: v1
kind: Service
metadata:
  name: aws-devops-poc
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
  selector:
    app: aws-devops-poc
